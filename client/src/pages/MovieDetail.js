import MovieInfor from "../components/MovieInfor";
import Template from "../components/Template";

const MovieDetail = () => {
  return (
    <Template children={<MovieInfor/>}/>
  );
};

export default MovieDetail;
